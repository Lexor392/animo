import type { FeedPreviewItem } from '@/features/posts/types/post.types';

export const getFeedPreview = async (): Promise<FeedPreviewItem[]> => {
  // Foundation placeholder. Real fetching should be implemented here, not in components.
  return [];
};
